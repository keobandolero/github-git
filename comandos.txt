git init --> para iniciar git.
git add (add .) --> para agregar todos los cambios
git commit -m "" 
git log 
git status
git diff "para ver los cambios"

git checkout"Para volver a la configuracion anterior claro sin haver dado commit"

git reset
git  log --graph --decorate --all --oneline
git config --global alias."" "sirve para agregar alias y o tener que estar escribiendo todo el codigo desde 0 ya que unas sentencias si son largar por ejemplo la de tree"

git reflog "para ver todo el historial de movimientos que e realizado"
git checkout "para regresar al movimiento segun el ash que demos"
git reset --hard "Para avanzar hacia adelante o hacia atras degun el ash que demos"

git tag "como bien se ve es para hacer una etiqueta asta cierto punto para ir haciendo separaciones"

git branch "" "Es para crear una nueva rama es decir partir desde un punto
en espesifico", (git branch -d "" para cargarnos la rama ).
git switch "branch creada" --> Es para movernos de rama en rama
git merge "Rama que se quiere unir" --> para unir ramas lo que se trabaja en una unirlo con la otra

"Para hacer un commit local"

git pull "Para hacer los cambios de que se han echo en git hub en la rama de arriba y traerlos a el git local"

git fetch "descargará el contenido remoto sin modificar el estado del repositorio local."

git push "se utiliza para comunicar con otro repositorio, calcular lo que tu base de datos local tiene que la remota no tiene, y luego subir (push) la diferencia al otro repositorio"



solo es para hacer un commit 

